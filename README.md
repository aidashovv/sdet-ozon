Приложение развернул в docker compose: настроил сервисы приложения, postgres и запуск миграций. Также добавил Makefile.

Курс валюты выдается через test_id в query-параметрах и только для 1 теста. За добавление теста в ситему отвечает ручка /setup. Извиняюсь за такую псевдо документацию, просто не успел внедрить Swagger.
Изоляция сценариев обеспечена через test_id, а консистентность данных — с помощью оптимистичных блокировок.

Проектировал домен, ориентируясь на расширяемость. В процессе реализации понял, что можно чуть иначе и лучше, но уже не успевал.

Хардкод есть только в **internal/mock_scenarios/presentation/mappers.go** (33-34 строки).
Хэдеры не использовал.

В .env.example оставил реальные данные для упрощения запуска и ревью.